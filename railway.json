{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd ridgeline-bot && npm install && npm run build"
  },
  "deploy": {
    "startCommand": "cd ridgeline-bot && node dist/migrate.js && node dist/index.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
