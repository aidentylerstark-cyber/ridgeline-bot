## Packages
framer-motion | Essential for smooth, minimal entry animations and list transitions
lucide-react | Clean, minimal iconography

## Notes
- "Clean minimal" aesthetic relies heavily on high contrast, generous whitespace, and pure monochrome palettes.
- We assume the backend serves `/api/items` with a JSON payload matching the schema.
